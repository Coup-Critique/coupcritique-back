when@dev:
    nelmio_cors:
        defaults:
            origin_regex: false
            allow_origin: ["*"]
            allow_headers: ["*"]
            allow_methods: ["GET", "OPTIONS", "POST", "PUT", "PATCH", "DELETE"]
            max_age: 3600
        paths:
            "^/": ~
